<!--
Pull requests for this macOS port target `main` and need at least one label
(see CONTRIBUTING.md → "PR requirements & CI"). If your change is to the shared
calculation engine or game data rather than the macOS host, please open it
upstream first so every platform benefits:
https://github.com/PathOfBuildingCommunity/PathOfBuilding-PoE2
-->

### What does this change, and why?

<!-- Brief summary. Link any issue with "Fixes #123". -->

### Area (add the matching label)

- [ ] macOS host (`macos/`) — rendering, fonts, input, networking
- [ ] Build / packaging (`tools/macos/`, CMake)
- [ ] CI / workflows
- [ ] Docs
- [ ] Engine sync (submodule bump)

### How was it tested on macOS?

<!-- e.g. built with tools/macos/build_app.sh and launched the app; ran tools/macos/tests/run.sh -->
-

### Screenshots (if the change is visible in the UI)
